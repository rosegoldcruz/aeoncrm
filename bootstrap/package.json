{
  "name": "aeon-bootstrap",
  "version": "1.0.0",
  "description": "AEON Dial Bootstrap - Deterministic Convergence Engine",
  "type": "module",
  "scripts": {
    "bootstrap": "npx tsx index.ts",
    "bootstrap:check": "npx tsx index.ts --check",
    "bootstrap:dry": "npx tsx index.ts --dry-run",
    "bootstrap:verbose": "npx tsx index.ts --verbose",
    "test": "npx tsx test.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.89.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
